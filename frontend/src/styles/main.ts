import { colors } from "./variables";

export default `
  .button {
    border-radius: 0;
    background: ${colors.electricViolet};
    box-shadow: none;
    color: #fff;
    min-width: 40px;
    min-height: 40px;
    padding: 5px 15px;
    font-size: 1rem;
  }

`;
